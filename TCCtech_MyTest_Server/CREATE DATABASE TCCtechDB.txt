CREATE DATABASE TCCtechDB;

USE TCCtechDB;

CREATE TABLE score_data (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    score INT NOT NULL,
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
);
